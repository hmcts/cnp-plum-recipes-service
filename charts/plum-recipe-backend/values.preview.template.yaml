java:
  # Don't modify below here
  image: ${IMAGE_NAME}
  ingressHost: ${SERVICE_FQDN}
  postgresql:
    enabled: true
  disableKeyVaults: true
  environment:
    POSTGRES_SSL_MODE: disable
    POSTGRES_PASSWORD: "{{ .Values.java.postgresql.auth.password}}"
    POSTGRES_DATABASE: "{{ .Values.java.postgresql.auth.database }}"
    POSTGRES_USER: "{{ .Values.java.postgresql.auth.username }}"
    POSTGRES_HOST: "{{ .Release.Name }}-postgresql"
